"""Dash et al. Pix2PixHD comparison implementation.

The Lightning entry point is ``module.DashPix2PixHD``. It is intentionally not
imported here so static tools can inspect the package without PyTorch.
"""
